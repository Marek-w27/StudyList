<UserControl x:Class="StudyList.ContactContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StudyList"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Imię" Width="100"/>
                <DataGridTextColumn Header="Nzwisko" Width="100"/>
                <DataGridTextColumn Header="Telefon" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" Margin="-10,329,10,91">
            <TextBlock Text="Imię" Margin="30,0"/>
            <TextBlock Text="Nazwisko" Margin="30,0"/>
            <TextBlock Text="Telefon" Margin="30,0"/>
            
            
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <Button Content="Dodaj" Margin="15"/>
            <Button Content="Usuń" Margin="15"/>
        </StackPanel>
    </Grid>
</UserControl>
